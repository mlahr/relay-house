services:
  relay-house:
    build: .
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - email-data:/app/data
    environment:
      DATABASE_PATH: /app/data/relay-house.db
    restart: unless-stopped

volumes:
  email-data:
